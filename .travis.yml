language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jswidget' ]; then cd .. && eval "mv $currentfolder generator-jswidget" && cd generator-jswidget; fi

